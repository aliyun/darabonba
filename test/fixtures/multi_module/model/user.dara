import DARAUtil;
import "../lib/util" Util;

model Info {
    name: string,
    age: integer,
}


static function test(): iterator[string]{
 var it:iterator[string] = Util.test1();
 for(var test : it) {
    yield test;
 }
}